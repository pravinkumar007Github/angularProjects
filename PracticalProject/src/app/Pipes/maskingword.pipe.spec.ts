import { MaskingwordPipe } from './maskingword.pipe';

describe('MaskingwordPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskingwordPipe();
    expect(pipe).toBeTruthy();
  });
});
